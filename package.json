{
  "name": "procatchem-lua-api-slate-docs",
  "version": "1.0.98",
  "private": true,
  "description": "Slate-style documentation site for the PROCatchem Lua Script API.",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "dev": "python3 -m http.server 8080 -d dist",
    "static": "python3 -m http.server 8080 -d dist",
    "slate:build": "bundle install && bundle exec middleman build",
    "slate:dev": "bundle exec middleman server"
  }
}
